# Copy to .env.local for machine-specific overrides (gitignored).
# Vite only exposes variables prefixed with VITE_.

# SMS backend API base URL (must include /api/sms path as used by httpClient)
VITE_SMS_API_BASE_URL=https://uat-sms.skyfi.com.vn/api/sms
